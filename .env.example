# Local development configuration.
# Copy this file to .env and replace the example values.
# NEVER commit the real .env file to GitHub.

SECRET_KEY=replace-with-a-random-secret-value
INVIGILATOR_EMAIL=invigilator@example.com
INVIGILATOR_PASSWORD=replace-with-a-strong-local-password
